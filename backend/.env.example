# Postgres provisto por el Marketplace de Vercel (integración Neon).
# Usar la connection string "pooled" en producción para funciones serverless.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
